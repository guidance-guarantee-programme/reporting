en:
  activerecord:
    errors:
      models:
        appointment_summary:
          attributes:
            completions:
              less_than_or_equal_to: must be less than 'transactions'
        uploaded_file:
          attributes:
            filename:
              invalid: File must have '.csv' extension
            data:
              headers: "Missing CSV headers: %{missing}"
              blank: File can not be empty
  activemodel:
    errors:
      models:
        twilio_numbers_report_request:
          attributes:
            email:
              invalid: Is not a valid email address
