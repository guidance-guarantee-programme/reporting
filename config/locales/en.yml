en:
  activerecord:
    errors:
      models:
        appointment_summary:
          attributes:
            reporting_month:
              invalid: does not match required format
            completions:
              less_than_or_equal_to: must be less than 'transactions'
        uploaded_file:
          attributes:
            filename:
              invalid: File must have '.csv' extension
            data:
              headers: "Missing CSV headers: %{missing}"
              blank: File can not be empty
